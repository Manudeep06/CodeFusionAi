# TODO - Fix VSCode C++ not working

- [ ] Inspect VSCode problem: verify build/run tasks and debug config for C/C++
- [ ] Add/update `.vscode/tasks.json` to compile C/C++ files with MinGW `g++`/`gcc`
- [ ] Update `.vscode/launch.json` to run the correct compiler output (`.exe`) for both C and C++
- [ ] Add problem matchers / ensure "Run" and "Debug" buttons work
- [ ] Test with `DSA/graph.cpp` and one C file (e.g., `input.c`)

